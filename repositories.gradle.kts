/**
 * Extra repository wiring (kept as a separate file like upstream Aniyomi extensions).
 *
 * The canonical repository list already lives in `settings.gradle.kts` via
 * `dependencyResolutionManagement`. This file exists so you can extend it later
 * without rewriting settings.
 */
